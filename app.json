{
  "name": "Telegram Auto Rights Monitor Bot",
  "description": "A silent Telegram bot that monitors its admin rights and removes members if ban permission is granted.",
  "repository": "https://github.com/yourusername/auto-rights-bot",
  "logo": "https://telegram.org/img/t_logo.png",
  "keywords": [
    "telegram",
    "telethon",
    "bot",
    "python",
    "heroku"
  ],
  "stack": "heroku-22",
  "env": {
    "BOT_TOKEN": {
      "description": "Your Telegram bot token from BotFather",
      "required": true
    },
    "API_ID": {
      "description": "Your Telegram API ID from my.telegram.org",
      "required": true
    },
    "API_HASH": {
      "description": "Your Telegram API hash from my.telegram.org",
      "required": true
    },
    "LOG_GROUP_ID": {
      "description": "Telegram group ID for bot logs (use -100 prefix for supergroups)",
      "required": true
    }
  },
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
